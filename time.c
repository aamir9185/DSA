#include<stdio.h>
#include<stdlib.h>

struct TIME{
    int hour;
    int minute;
    int second;
    struct TIME *next;
}
void displayTime(struct TIME *firstTime){
    while (firstTime == NULL){
        printf("%02d : %02d : %02d")
    }
    {
        /* code */
    }
    
}